#include "main.h"
#include "main.h"
/**
 * _abs  -  function to abslouite the numbers
 *
 * @n : checks input of function
 *
 * Return: returns to "n"
 */
int _abs(int n)
{
if (n < 0)
{
n = n * -1;
}
else
{
n = n * 1;
}
return (n);
}
